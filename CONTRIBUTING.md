# 贡献指南

先说清楚这个项目的取舍，免得白做工。

## 项目的立场

这个工具存在的理由是**回答「这套判断逻辑到底赚不赚钱」**，
不是「再多接一个数据源」或「把看板做得更好看」。

所以：

- **欢迎**：让验证更严谨、让回测更可信、修数据正确性的 bug、补测试
- **不欢迎**：加花哨的推送通道、加没有历史数据支撑的指标、
  在回测里引入当时拿不到的信息（基本面、新闻、资金流）

## 开发环境

```bash
git clone https://github.com/AandBorC/dsa-lite.git
cd dsa-lite

# 零依赖也能跑
python tests/selftest.py
python tests/test_adjust.py

# 可选：读 config.yaml 需要 pyyaml
pip install pyyaml
```

## 提交前必须过

```bash
python tests/selftest.py      # 53 项断言
python tests/test_adjust.py   # 10 项断言
python main.py --help
```

这两套测试是**离线的**，不联网、不调 LLM。CI 会在
Python 3.10 / 3.12 / 3.13 三个版本上各跑一遍，
并且有一条独立流水线在**完全不装第三方包**的环境里跑 ——
用来守住「零强制依赖」这个对外承诺。

## 硬性红线

1. **绝不提交凭证**。`.env`、token、webhook、密钥一律走环境变量或 GitHub Secrets。
2. **绝不把自选股/持仓硬编码进 `.yml`**。Secrets 不公开，但写进 workflow 就是公开的。
3. **报告里不出现本机绝对路径**。项目内的路径统一用 `main._rel()` 相对化。
4. **改了策略逻辑，必须贴 `validate` 的前后对比**，包括样本外表现。
   只给样本内的漂亮数字等于没给证据。

## 谈到策略有效性时，用这套口径

| 指标 | 及格 | 优秀 |
|------|------|------|
| 胜率 | >45% | >55% |
| 盈亏比 | >1.5:1 | >2.5:1 |
| 最大回撤 | <20% | <12% |
| 夏普 | >1.0 | >1.5 |

另外三条是门槛，不是加分项：

- 交易笔数 **< 20 笔**：直接判「样本不足」，不给任何有效性结论
- **样本外期望衰减 > 60%**：判过拟合，策略不可用
- **p > 0.05**：不显著，等于没法排除运气

## PR 提交流程

1. Fork → 建分支（`fix/xxx`、`feat/xxx`）
2. 改完跑一遍上面的测试
3. 按 PR 模板填，动策略必须附对比表
4. 说明**为什么**这么改，而不只是改了**什么**

## 报告数据源问题

先跑 `python main.py doctor`，把输出附上（记得删掉 token 和本机路径）。
它能直接区分：没装 / 没配 token / 积分不够 / 被限流 / 网络不通。
