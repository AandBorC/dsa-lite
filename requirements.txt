# dsa-lite 依赖
# ============================================================
# 核心功能零依赖 —— 纯标准库即可运行（行情走东方财富裸 HTTP 接口）
# 下面这些全部是可选的，装了才启用对应能力，不装不影响主流程
# ============================================================

# --- 需要 yaml 格式的 config.yaml 时才装 ---
pyyaml>=6.0

# --- 备用数据源（东财接口挂了的时候顶上）---
# akshare>=1.12.0
# tushare>=1.4.0
# baostock>=0.8.8
